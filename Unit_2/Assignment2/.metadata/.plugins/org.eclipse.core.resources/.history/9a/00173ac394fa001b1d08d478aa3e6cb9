#include <stdio.h>
#include <ctype.h>

int main (){

	int in;

	printf("Enter an integer: ");
	fflush(stdout); fflush(stdin);

	scanf("%d",&in);

	int fact = 1;

	if(in == 0)
		printf("Factorial = %d", 1);
	return 0;
}
